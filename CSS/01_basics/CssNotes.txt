Inline styling - inline styling means witin the line.
class and id are more powefull in css.
Flexbox : Flex box is a thing which allows you postion at certain element  postion on web page.
Selectors: Selectors is the ability to select any element on the webpage.

Inline element: Inline element don't consume it's own space
 
Block element: Block level element takes full width from left to right